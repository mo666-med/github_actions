name: CI

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  node-ci:
    uses: mo666-med/github-actions-node-ci/.github/workflows/node-ci-reusable.yml@v1
    with:
      node-version: '20'
      working-directory: '.'
      run-lint: true
      run-test: false
      run-build: true
